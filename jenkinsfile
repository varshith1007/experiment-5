pipeline {
    agent any

    stages {
       stage('compile') {
           steps {
               sh 'hello.java'
            }
      }

      stage('run') {
          steps {
              sh 'java addition'
          }
      }
  }
}
